# The Odds API — https://the-odds-api.com/
# Used by src/odds/fetch.py to pull batter_home_runs_alternate (point=0.5, Over) from FD + DK.
ODDS_API_KEY=your_key_here

# Optional: override default cache locations
# HR_V7_DATA_DIR=./data
